name = "Just Enough Items (JEI)"
filename = "jei-1.21.1-neoforge-19.53.0.425.jar"
# BOTH sides, and it must stay that way. JEI's "+" recipe-transfer button is
# executed server-side: BasicRecipeTransferHandler checks
# IConnectionToServer.isJeiOnServer() and, when false, refuses with
# jei.tooltip.error.recipe.transfer.no.server. The item movement is done by
# mezz.jei.common.transfer.BasicRecipeTransferHandlerServer handling
# PacketRecipeTransfer / PacketRecipeTransferCounted, which only exists if the
# server has the jar. Same for cheat mode (PacketGiveItemStack ->
# ServerCommandUtil). This was briefly set to side = "client" and the "+" button
# did not work on the server as a result.
#
# The client-facing addons stay client-only -- JER, JEP and JECT are pure UI and
# register nothing server-side; their manifests' side = "BOTH" refers to the
# dependency, not to the mod.
#
# No longer pinned. It was held at 19.39.0.372 while the pack was on NeoForge
# 21.1.234, because every build from 19.42.0.379 on requires [21.1.238,); the
# 2.1.0 bump to 21.1.249 cleared that.
#
# Why not EMI: mods here register recipe-transfer handlers against the JEI API
# only. Visual Workbench is the concrete case -- it replaces the crafting table
# menu with VisualCraftingMenu and ships VisualWorkbenchJEIPlugin. EMI has no
# equivalent, so its "+" fill button reported "not enough ingredients" at every
# crafting table, and the JEMI bridge cannot cover it: EMI implements only
# IModIngredientRegistration, IRuntimeRegistration and ISubtypeRegistration --
# IRecipeTransferRegistration is not bridged at all.
side = "both"

[download]
url = "https://cdn.modrinth.com/data/u6dRKJwZ/versions/5ULZJRXC/jei-1.21.1-neoforge-19.53.0.425.jar"
hash-format = "sha512"
hash = "0b847b07a27ea1c825ed022b94ed6e5154a54809d8d505dadfe3326844e2a210c996ce2eb7eb705809a1dc79fd5774b51b46762a07fa969b7edf8e059bb6ee08"

[update]
[update.modrinth]
mod-id = "u6dRKJwZ"
version = "5ULZJRXC"
